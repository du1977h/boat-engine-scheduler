あなたは、要件定義・アーキテクチャ設計・DB設計・UI設計・実装・テスト・README作成・運用ドキュメント作成まで一貫して行う、熟練のフルスタックエンジニアです。
以下の要件に従って、部活の道具運搬担当スケジュールを管理する日本語対応PWAを、実運用可能な品質で構築してください。

【最重要方針】
単なるモックではなく、実際に起動・運用できるアプリ一式を実装すること
単一VPS / Node.js直接起動 / SQLite / systemd / nginx / HTTPS 前提で設計すること
スマホ最優先でUI設計すること
管理者CSV・部員CSVを取り込み → ログイン → 月カレンダー編集 → 集計 → CSV出力 の一連の動作が成立すること
実装は過度に複雑にしないこと
UIは日本語にすること
未確定事項は以下の優先順位で合理的に補完すること
スマホで使いやすいこと
VPSで運用しやすいこと
保守しやすいこと
実装が過度に複雑にならないこと
【ゴール】

部活で使用する以下2種類の道具について、月ごとのカレンダー形式で担当管理できるPWAを作成すること。

対象物
ボート
エンジン
管理する業務
自宅から部活動への運搬
部活動から自宅への運搬
道具の保管
日付ごとに記録したい内容

各入力日は日付単位で扱うこと。
任意の日付について、以下を記録できること。

対象物: ボート / エンジン
役割: 保管 / 行き / 帰り
担当者: 部員マスタから選択

つまり、1日あたり最大6項目を編集できること。

ボート × 保管担当
ボート × 行き担当
ボート × 帰り担当
エンジン × 保管担当
エンジン × 行き担当
エンジン × 帰り担当

各入力欄の担当者は、部員CSVから取り込んだ部員マスタからプルダウン選択すること。

【利用・運用前提】
利用環境
スマホ最優先
PCでも閲覧・編集可能
複数人利用
同時編集の厳密な競合解決は不要
オフライン編集は不要
PWAとしてホーム画面追加できることは必須
公開・インフラ
公開先はレンタルVPS
Dockerは使わない
Node.jsアプリを直接起動する
DBはSQLite
単一サーバー、単一SQLiteファイル運用
プロセス管理はsystemd
HTTPS必須
nginxをリバースプロキシとして採用すること
READMEにnginx設定例を必ず含めること
READMEにLet's Encrypt等によるHTTPS化手順例を必ず含めること
本案件の公開例では `mw1.sailripper.top` を用い、他アプリが `3000` を使用している前提なら本アプリは `3100` で待ち受けて nginx から公開する手順をREADMEに明記すること
ユーザー運用
ログイン方式は「事前設定した login_id + password」
ユーザーは管理者がCSVで一括登録する
パスワードはCSVに初期値として含めてもよい
ただし保存時は必ずハッシュ化する
初回ログイン時パスワード変更は必須ではない
ログイン画面で入力するのは display_name ではなく login_id とすること
CSVファイルを編集しただけではDBに反映されないため、運用手順やREADMEでは「編集後に再インポートが必要」と明記すること
権限は以下の2種類
管理者
一般
管理者ができること
ユーザーCSV取込
部員CSV取込
部員一覧確認
ユーザー一覧確認
取り込み結果確認
過去データ修正
予定入力
一般ユーザーも以下は可能
全月・全担当の編集
集計閲覧
CSV出力
更新履歴は最低限以下のみ保持
最終更新者
最終更新日時
過去月の参照可能
前月コピー機能は不要
【確定済み仕様】

以下は確定事項として扱うこと。

入力単位は日付単位
役割は3種類固定
保管担当
行き担当
帰り担当
月カレンダー・入力画面・CSV・DB設計はすべて日付単位に統一
月担当表CSVは日付単位で出力
DBモデルは日付単位
集計は3役割ベース
users と members は別管理
担当者プルダウンは有効な部員のみ表示
CSV取込方式
部員CSV: 全件入れ替え方式（CSVにない既存部員は無効化）
ユーザーCSV: 追加・更新のみ（CSVにない既存ユーザーは残す）
【必須機能要件】
1. 認証

以下を必ず実装すること。

ログイン画面
login_id と password による認証
認証後のセッション維持
ログイン中ユーザー情報取得APIまたは同等機構
権限別アクセス制御
未ログイン時は保護画面にアクセス不可
一般ユーザーは管理者専用機能にアクセス不可

認証方式は、Node.js / React / Next.js 系で保守しやすく、VPS上で安定運用しやすい構成を採用すること。

2. 初期セットアップ

初期セットアップでは、ユーザーCSVを読み込んで初期ユーザーを一括登録できること。

READMEには必ず以下を明記すること。

初回セットアップ手順
DB初期化手順
管理者CSV投入手順
部員CSV投入手順
環境変数設定方法
本番起動方法

初期化処理は以下どちらでもよい。

CLIスクリプト
管理者限定画面

ただし、実運用しやすい方式を採用し、READMEに具体手順を書くこと。

3. CSV取り込み機能
3-1. 部員CSV

部員CSVは最低限「氏名」列のみでよい。
初回セットアップまたは管理画面から取り込めるようにすること。

要件
固定列形式
見本CSVを同梱すること
1件でも不備があれば全体取り込み中止
不備内容を日本語で分かりやすく表示
重複氏名や空欄を検証すること
取り込み時は全件入れ替え
CSVに存在しない既存部員はis_active = false とする
過去の担当データ表示は壊さないこと
必要ならインポート履歴を保持してよい
3-2. ユーザーCSV

以下の列を最低限扱えること。

login_id
password
display_name
role
要件
固定列形式
見本CSVを同梱すること
1件でも不備があれば全体取り込み中止
password は保存時に必ずハッシュ化
role は「管理者」「一般」のいずれか
login_id 重複を防ぐ
エラー内容は日本語表示
取り込み時は追加・更新のみ
CSVに存在しない既存ユーザーは削除・無効化しない
サンプルCSVや運用説明では、CSV編集後に再インポートしない限りログイン情報は変わらないことを明記すること
4. 月カレンダー画面

ログイン後の初期画面は当月カレンダーにすること。

要件
年月切替可能
指定月に含まれるすべての日を対象として扱う
カレンダー上に最低限以下を表示すること
入力済み / 未入力
選択中の対象物の 保管 / 行き / 帰り の担当者概要
スマホ優先で視認性を確保すること
必要に応じてスマホ向けに1か月表示の見せ方を工夫してよい
推奨UI
月カレンダーの各日セルは小さくなりすぎないこと
スマホでは各日セルに詳細を詰め込みすぎず、対象物切替 + 要約表示 + タップで詳細編集にすること
入力済み件数や対象物ごとの状態表示で判読性を確保すること
カレンダーはボート/エンジン切替式とし、ボートは RGB(245,241,89) ベースの黄色系、エンジンは灰色系で視認できるようにしてよい
土曜・日曜・祝日のパネルは薄い赤色背景にしてよい
カレンダーページ上部には「琉球大学医学部ボードセイリング部」「ボート用具運搬担当表」を表示してよい
5. 日別入力画面

月カレンダーの日付を選択したら、その場でフロート表示される日別入力UIを開けること。
別画面遷移でもよいが、スマホで素早く編集しやすい設計を優先すること。

入力単位
各日
対象物
ボート
エンジン
役割
保管担当
行き担当
帰り担当
表示レイアウト
現在カレンダーで選択している対象物のみ表示
その対象物について3役割分の入力欄を持つ
各入力欄はプルダウン
候補は部員マスタから取得
候補表示対象は is_active = true の部員のみ
検索機能は不要
未入力時は「未選択」と表示すること
保存仕様
プルダウン選択時に即保存
自動保存
保存成功 / 保存失敗が分かるUIを表示
過去データを読み込んで編集可能
当月以外も編集可能
データルール
1つの役割に複数人は不可
同じ人が同じ日に複数役割を兼任することは許可
保管に入力がある場合は 行き/帰り を入力不可にすること
行きまたは帰りに入力がある場合は 保管 を入力不可にすること
未選択に戻したら再入力可能にすること
6. バリデーション
必須バリデーション
必須項目未選択は未入力扱い
CSV必須列欠落
CSV空欄
login_id重複
member_name空欄または重複
role不正値
UI方針
エラーと警告は明確に区別
エラーは赤系
日本語で分かりやすい文言
7. 集計機能

集計は単月単位のみでよい。

集計条件
月を選択して担当実績を集計
1部員 = 1行表示
並び順:
総担当回数の多い順
同数の場合は氏名の五十音順
集計列
ボート保管回数
ボート行き回数
ボート帰り回数
エンジン保管回数
エンジン行き回数
エンジン帰り回数
並び順の判定には総担当回数を内部的に用いてよい
UI
スマホでも見やすいテーブル
必要なら横スクロール可
実装しやすい範囲でヘッダー固定やカード表示を採用してよい
回数集計とは別に、以下の単価を掛けた支払額集計表を追加してよい
ボート保管: 50円/回
ボート行き: 300円/回
ボート帰り: 300円/回
エンジン保管: 50円/回
エンジン行き: 200円/回
エンジン帰り: 200円/回
8. CSV出力
8-1. 月担当表CSV

1行 = 1役割単位の詳細レコード

最低限含める列
year
month
target_date
item_type
role_type
member_name
updated_by
updated_at
値例
item_type: boat / engine
role_type: storage / go / return
8-2. 月集計CSV

1行 = 1部員

最低限含める列
member_name
boat_storage_count
boat_go_count
boat_return_count
engine_storage_count
engine_go_count
engine_return_count
9. 管理機能

管理者専用画面を作成し、最低限以下を実装すること。

部員CSV取込
ユーザーCSV取込
部員一覧表示
ユーザー一覧表示
取り込み結果確認
最終更新者 / 最終更新日時確認
過去データ修正
月担当表CSVダウンロード

一般ユーザーは管理画面・管理APIに入れないこと。

【UI / UX要件】
UI文言は日本語
シンプルで見やすさ重視
スマホで押しやすいサイズ
カレンダー上の判読性を重視
ログイン後は当月カレンダーを開く
主要導線として以下へアクセスしやすくすること
カレンダー
集計
CSV出力
管理
CSV出力画面では「担当回数集計CSV」「支払額集計CSV」を明示的なダウンロードボタンで表示してよい
月担当表CSVは管理画面に配置してよい
上部メニュー / 下部タブ / ハンバーガー等は合理的に選択してよい
派手さより実用性優先
フォーム部品はモバイルで誤操作しにくいこと
【技術選定の制約】

技術選定は自由だが、以下を満たすこと。

PWAとして実装
スマホ向けUIを作りやすい
VPSでNode.jsアプリを直接動かしやすい
SQLiteとの相性がよい
認証実装しやすい
保守しやすい
READMEでセットアップしやすい
実運用を考えて過度に複雑にしない
推奨
React / Next.js 系
ORM利用
バリデーションライブラリ利用
PWA manifest / service worker 実装
HTTPS前提
実装開始前に最初に必ず提示すること
採用技術構成
採用理由
【データモデル要件】

最低限、以下の概念を持つこと。

users
members
schedule_days
assignments
import_histories

項目名は実装しやすく調整可。ただし意図は満たすこと。

users

例:

id
login_id
password_hash
display_name
role
created_at
updated_at
members

例:

id
name
is_active
created_at
updated_at
schedule_days

例:

id
target_date
year
month
created_at
updated_at
assignments

例:

id
schedule_day_id
item_type
role_type
member_id
updated_by_user_id
updated_at
created_at
要件
同じ日付・同じ item_type・同じ role_type には複数担当が入らないよう一意制約を設計すること
未入力状態を扱いやすいようにすること
履歴テーブルは必須ではないが、最低限「最終更新者」「最終更新日時」は取得可能にすること
import_histories

例:

id
import_type
file_name
row_count
success
executed_by_user_id
executed_at
error_summary
【データ設計上の運用ルール】
users と members は別管理
ログインユーザーと担当者マスタは自動紐付けしない
担当者プルダウンには is_active = true の部員のみ表示する
過去データに紐づく無効化済み部員は、表示上は名前を維持できるようにすること
部員CSV取込は全件入れ替えだが、過去担当実績を壊さないこと
ユーザーCSV取込は追加・更新のみで、既存ユーザーを消さないこと
【セキュリティ要件】
パスワード平文保存禁止
ハッシュ化必須
認証保護必須
入力値バリデーション必須
CSVアップロード時バリデーション必須
一般ユーザーが管理画面や管理APIへアクセス不可
CSRFやセッションの基本対策を考慮
エラーメッセージに不要な内部情報を出さない
READMEに本番運用時の注意点を記載すること
【README に必ず含める項目】

README.md には最低限以下を含めること。

アプリ概要
ユースケース
採用技術一覧
技術選定理由
ディレクトリ構成
画面一覧
機能一覧
DB設計概要
ローカル開発手順
必要なNode.jsバージョン
環境変数一覧
DB初期化手順
管理者CSV投入方法
部員CSV投入方法
サンプルCSVの説明
月担当表CSV出力仕様
月集計CSV出力仕様
本番デプロイ手順
systemd設定例
nginxリバースプロキシ設定例
HTTPS化手順例
`mw1.sailripper.top` での公開手順例
SQLiteファイル配置の考え方
バックアップの考え方
セキュリティ上の注意点
既知の制約
今後の改善案

READMEは省略せず丁寧に書くこと。

【納品物】

以下をすべて含めること。

アプリ本体一式
DBスキーマ / マイグレーション
サンプル部員CSV
サンプルユーザーCSV
README.md
nginx設定例
systemdユニットファイル例
必要に応じた初期データ投入スクリプト
PWA設定一式
必要に応じたテストコード
【実装ルール】
できる限り実際に動作する構成で実装すること
ダミーや雛形ではなく、実用的な粒度で書くこと
未実装箇所がある場合は TODO と理由を明記すること
単一VPS・SQLite運用に合った現実的設計にすること
UIはスマホ優先
API・DB・UI・バリデーション・認証・CSV入出力を一貫して実装すること
【出力順序】

以下の順序で出力すること。

採用技術構成
採用理由
ディレクトリ構成
DB設計
主要画面設計
API設計
実装コード一式
サンプル部員CSV
サンプルユーザーCSV
README.md
nginx設定例
systemd設定例
今後の改善案
【コード出力ルール】
省略せず、できるだけ実ファイル単位で出力すること
各コードブロックの前に必ずファイルパスを明記すること
可能な限りそのまま配置して動かせる形にすること
環境変数が必要なら .env.example を出力すること
マイグレーションやスキーマも必ず含めること
サンプルCSVは実際に読み込める内容にすること
READMEは手順省略なしで書くこと
【判断を委ねる項目】

以下は合理的に選定してよい。

Next.js / React構成
UIライブラリ
ORM
認証ライブラリ
CSVパーサ
日付ライブラリ
service worker実装方式
エラーメッセージ文言
バックエンド構成
画面遷移方式
コンポーネント分割

ただし、選定理由を README に必ず記載すること。

【受け入れ条件】

以下を満たしていれば完了とみなす。

単一VPS上でNode.jsアプリとして起動できる
SQLiteで永続化できる
ユーザーCSVを投入してログインできる
部員CSVを投入できる
月カレンダーで日ごとの入力状況が確認できる
日別入力画面でボート/エンジン各3役割を編集できる
プルダウン変更時に自動保存される
警告とエラーが区別表示される
月次集計が表示できる
月担当表CSV / 月集計CSV を出力できる
管理者専用機能にアクセス制御がかかっている
PWAとして manifest / service worker / installability を備えている
READMEにセットアップから本番運用までの具体手順がある
nginx/systemd/HTTPSの設定例がある
【重要な実装上の補足】
実装はシンプルさ優先
スマホUIは誤操作しにくく、最小タップ数で編集できること
厳密なリアルタイム共同編集は不要
オフライン編集は不要
日別入力UIは、月カレンダーから素早く開いてすぐ閉じられること
CSV取込エラーは、ユーザーが修正しやすいように行番号・列名・原因を日本語で表示すること
保存APIやCSV取込APIは、実運用を前提に失敗時の日本語メッセージを丁寧に設計すること
